# uv Role

This role manages the installation and configuration of uv and related pip/direnv integration.

## Variables
- See `defaults/main.yml` and `vars/` for available variables.

## Usage
Include this role in your playbook:
```yaml
- role: uv
```
