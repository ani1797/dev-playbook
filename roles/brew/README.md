# Brew Role

This role manages Homebrew packages and related configuration.

## Variables
- See `defaults/main.yml` and `vars/` for available variables.

## Usage
Include this role in your playbook:
```yaml
- role: brew
```
