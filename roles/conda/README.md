# Conda Role

This role manages Conda configuration and setup.

## Variables
- See `defaults/` and `vars/` for available variables.

## Usage
Include this role in your playbook:
```yaml
- role: conda
```
