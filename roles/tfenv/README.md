# tfenv Role

This role manages the installation and configuration of tfenv and its integration with direnv.

## Variables
- See `defaults/` and `vars/` for available variables.

## Usage
Include this role in your playbook:
```yaml
- role: tfenv
```
