# Git Role

This role manages Git configuration and related shell integration.

## Variables
- See `defaults/main.yml` and `vars/` for available variables.

## Usage
Include this role in your playbook:
```yaml
- role: git
```
